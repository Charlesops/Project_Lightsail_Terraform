terraform {
  required_providers {
    aws = {
        source = "hashicorp/aws"
 dev
        version = "4.61.0"

        version "4.60.0"
 main
    }
  }
}

# This line is inactive 

provider "aws" {
    region = "us-east-é"
}